export const DEPLOY_DOMAIN = "amberd.ai";
export const DEPLOY_BRANCH_REPO = "dealership_new";

/** Display labels for tier indices 0–3 (matches metrics API tier keys). */
export const TIER_DISPLAY_NAMES = [
  "Tier 1",
  "Tier 2",
  "Tier 3",
  "Tier 4",
] as const;
